doctype html
html(lang="ja")
    head
        meta(charset="utf-8")
    body
        if user
            nav 
                span #{user.username}
                br
                img(src=user.avatar_path width="50" height="50")
        else 
            nav
                a(href="/signin") ユーザー登録
                br
                a(href="/login") ログイン
        a(href="./update") 投稿
        h1 投稿一覧
        section
            each msg in messages
                div 
                    h2 #{msg.username}
                    time #{msg.date}
                    p #{msg.message}
                if msg.image_path   
                    img(src=msg.image_path width="100" height="100")